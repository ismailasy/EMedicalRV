<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emedicalrv.sn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListConsultActivity"></activity>
        <activity android:name=".CreateConsultActivity" />
        <activity android:name=".CreateRdvActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".HospitalActivity" />
        <activity android:name=".ConsultationActivity" />
        <activity android:name=".AppointmentActivity" />
        <activity android:name=".PatientActivity" />
        <activity android:name=".PresentationActivity" />
        <activity android:name=".ListRdvActivity" />
        <activity android:name=".AcceuilActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>